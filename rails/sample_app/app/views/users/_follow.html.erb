<%= form_for(current_user.active_relationships.build, remote: true) do |f| %>
  <div><%= hidden_field_tag :followed_id, @user.id %></div> <!-- will send followed_id param with the value of id of the previewed user (id param of current url) to RelationshipController#follow action -->
  <%= f.submit "Follow", class: 'btn btn-primary' %>
<% end %>
