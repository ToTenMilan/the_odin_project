<%= form_for(current_user.active_relationships.find_by(followed_id: @user.id), html: {method: :delete}, remote: true) do |f| %> <!-- will send previewed user id to params to use it in RelationshipsController#unfollow action -->
  <%= f.submit "Unfollow", class: 'btn' %>
<% end %>
